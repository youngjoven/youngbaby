# 소스 코드 (Source Code): 영아 분유 수유 관리 애플리케이션

**Role:** AWS Solution Architect
**참조:** 1.inception.md, 2.domain.md, 3.logical.md

---

## 체크리스트

### iOS 앱
- [x] Xcode 프로젝트 생성 및 구조 설정
- [x] SwiftData 모델 구현 (UserProfile, FeedingRecord, BowelRecord)
- [x] 온보딩 화면 구현 (아이 이름·생년월일·어머니 이름 입력, 로컬 저장)
- [x] Cognito 회원가입/로그인/이메일인증 화면 구현 (AuthView, CognitoService, AuthManager)
- [x] 홈 탭 구현 (오늘 요약, 수유·배변 기록 버튼, 어드바이저 추천 카드)
- [x] 기록 탭 구현 (수유·배변 기록 목록, 통계 카드, 삭제 기능)
- [x] 어드바이저 탭 구현 (AI 추천 카드 조회 UI)
- [x] 인사이트 탭 구현 (주간 인사이트 카드 피드 UI)
- [x] 월령 자동 계산 로직 구현 (AgeCalculatorService)
- [x] 수유 통계 계산 로직 구현 (FeedingService)
- [x] AWS API 연동 활성화 (APIService → AppConfig.apiBaseURL, Cognito IdToken 인증)
- [ ] 오프라인 우선 동기화 로직 활성화 (syncedAt 기반) — 실기기 테스트 후 진행

### AWS 백엔드 (Terraform)
- [x] Terraform Bootstrap 구성 (S3 상태 버킷 + DynamoDB 잠금 테이블)
- [x] VPC Private 아키텍처 구성 (10.100.0.0/16, us-east-1a/b, IGW/NAT 없음)
- [x] VPC Endpoint 구성 (Gateway: S3/DynamoDB 무료, Interface: Bedrock/SNS/CloudWatch/EventBridge)
- [x] WAF 구성 (Rate 30req/min, OWASP, IP Reputation)
- [x] Cognito User Pool + Client 생성
- [x] DynamoDB 테이블 5개 생성 (feedings, bowels, profiles, insights, device_tokens)
- [x] IAM 역할 구성 (Lambda 실행 역할, EventBridge Scheduler 역할)
- [x] Lambda 함수 8개 배포 (/feedings, /bowels, /profile, /advisor, /insights, /alarm, /alarm_delivery, /device)
- [x] API Gateway (REST API + Cognito Authorizer + 12개 엔드포인트)
- [x] AWS 인프라 배포 완료 (`terraform apply` — 98 리소스)
- [x] iOS Config.xcconfig 생성 (BabyDiary/Config.xcconfig)
- [ ] Xcode Build Settings에 Config.xcconfig 연결 — 수동 작업 필요
- [ ] APNs .p8 키 발급 및 SNS Platform Application 설정 — Apple Developer 등록 후 진행

### 소스 코드 검토 및 승인
- [x] iOS 앱 소스 코드 검토 및 승인
- [x] AWS 백엔드 Terraform 코드 검토 및 승인
- [x] AWS 인프라 배포 검증 완료 (VPC, Endpoint 6개, Lambda LogGroup 8개)
- [x] iOS ↔ AWS 코드 연동 완료 (Auth 흐름, APIService, AppConfig)
- [ ] 실기기 통합 테스트 — 진행 필요

---

## 개발 순서

```
[완료] 1단계: Xcode 프로젝트 + SwiftData 모델
[완료] 2단계: 온보딩 화면 (로컬 저장)
[완료] 3단계: 홈 탭 (수유·배변 기록, 오늘 요약, 어드바이저 카드)
[완료] 4단계: 기록 탭 (통계 카드, 기록 목록, 삭제)
[완료] 5단계: 어드바이저 탭 + 인사이트 탭 UI
[완료] 6단계: Lambda 함수 코드 작성 (Python 8개)
[완료] 7단계: Terraform 인프라 코드 작성 + AWS 배포 (98 리소스)
[완료] 8단계: AppConfig.swift 생성 (AWS 실제 배포 값, xcconfig 대신 Swift 파일 방식)
[완료] 9단계: Cognito 로그인/회원가입/인증 화면 추가 (AuthView, CognitoService, AuthManager)
[완료] 10단계: API 연동 활성화 (APIService → AppConfig, RootView 인증 흐름 추가)
[진행 필요] 11단계: 실기기 통합 테스트 (Cognito 회원가입 → 수유기록 → AI 추천)
[추후] 12단계: APNs 설정 (Apple Developer 등록 후)
```

---

## 질의응답

**[Question] 1:** 앱 번들 ID를 결정해야 합니다. App Store 등록 시 사용되는 고유 식별자입니다.
예시: `com.youngbaby.app`, `com.{이름}.youngbaby`
**[Answer]** 네, com.youngbaby.app 이것으로 결정하겠습니다.

**[Question] 2:** 앱 표시 이름(App Display Name)을 결정해야 합니다. 홈 화면에 보이는 앱 이름입니다.
예시: `아기 수유`, `영아 수유 기록`, `밀크 노트`
**[Answer]** '아기 일기장'으로 이름을 짓습니다.

**[Question] 3:** AWS 리전(Region)을 결정해야 합니다.
**[Answer]** us-east-1 (버지니아 북부)으로 전체 통합. Bedrock 동일 리전 사용을 위해 VPC Private 환경에서 모든 서비스를 단일 리전으로 통합 결정. (초기 ap-northeast-2 → us-east-1로 변경)
